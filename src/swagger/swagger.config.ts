import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: ' Company Service',
  description: " Company Backend API's Service",
  version: '1.0',
  tags: ["Back End API's Service"],
};
